#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a;
    cin >> a;

    if (a > 1)
    {
        cout << a << " is greater than 1.";
    }
    else
    {
        cout << a << " is less than or equal.";
    }
    return 0;
}